export type SKColor = {
    _class: 'color' // 颜色类型组件
    red: number // 取值范围[0<= val <=1]
    green: number // 取值范围[0<= val <=1]
    blue: number // 取值范围[0<= val <=1]
    alpha: number // 取值范围[0<= val <=1]
}
