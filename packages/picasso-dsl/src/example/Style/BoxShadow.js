const Color = require('./Color')

module.exports = [
  {
    offsetX: offX,
    offsetY: offY,
    spread: shadow.spread,
    blurRadius: shadow.blurRadius,
    color: Color,
    type
  },
  {
    offsetX: offX,
    offsetY: offY,
    spread: shadow.spread,
    blurRadius: shadow.blurRadius,
    color: Color,
    type
  }
]
