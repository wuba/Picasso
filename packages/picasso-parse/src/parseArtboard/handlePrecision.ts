import { SKLayer } from '../types';

/**
 * @description 数字精度处理
 * @param {SKLayer[]} layers
 * @returns {SKLayer[]}
 */
const handlePrecision = (layers:SKLayer[]):SKLayer[] => {
    return [];
}

export default handlePrecision;
